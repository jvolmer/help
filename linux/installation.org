# add trash to dock
gsettings set org.gnome.shell.extensions.dash-to-dock show-trash true

# each workspace only shows its own active applications in dock
gsettings set org.gnome.shell.extensions.dash-to-dock isolate-workspaces true

# workspace switching happens on all screens
gsettings set org.gnome.mutter workspaces-only-on-primary false


# infos: itsfoss.com/fix-ubuntu-freezing/

# disable it
# Problems: strange errorsmessages when starting ubuntu, lower screen brightness, external monitor via HDMI not working
sudo gedit /etc/default/grub
GRUB_CMDLINE_LINUX_DEFAULT="quiet splash" -> GRUB_CMDLINE_LINUX_DEFAULT="quiet splash nomodeset"
sudo update-grup


# install driver
in Software & Updates -> Additional Drivers -> select tested NVIDIA driver -> Apply Changes -> Restart
installed nvidia-384 (because of error when trying to install kubuntu previously)
